{
  "name": "@airclear/standard-version-updater-pom",
  "version": "1.0.1",
  "private": false,
  "publishConfig": {
    "access": "public"
  },
  "description": "The conventional-changelog/standard-version updater for Pom.xml build files.",
  "keywords": [
    "conventional-changelog",
    "java",
    "pom",
    "gradle",
    "standard-version",
    "standard-version-updater",
    "updater"
  ],
  "author": "airclear",
  "contributors": [],
  "license": "UNLICENSE",
  "repository": {
    "type": "git",
    "url": "https://github.com/airclear/standard-version-updater-pom.git"
  },
  "bugs": {
    "url": "https://github.com/airclear/standard-version-updater-pom/issues"
  },
  "files": [
    "dist"
  ],
  "main": "dist/build-pom.js",
  "types": "dist/build-pom.d.ts",
  "engines": {
    "node": ">=10.0.0",
    "npm": ">=5.6.0"
  },
  "devDependencies": {
    "@damlys/ts-sdk": "^1.0.1"
  },
  "scripts": {
    "build": "rm -rf ./dist && tsc",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint './**/*.{ts,tsx}' --fix",
    "lint:check": "eslint './**/*.{ts,tsx}'",
    "test": "jest --verbose",
    "test:coverage": "rm -rf ./coverage && jest --coverage",
    "test:watch": "jest --watchAll",
    "publish": "npm publish --registry=http://registry.npmjs.org"
  }
}
